# =============================================================================
# src/types/CMakeLists.txt — Type system sources
# =============================================================================

target_sources(hpactor_lib PRIVATE
    failure_reason.cpp
    typed_message.cpp
)
