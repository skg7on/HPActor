# =============================================================================
# src/rpc/CMakeLists.txt — RPC subsystem sources
# =============================================================================

target_sources(hpactor_lib PRIVATE
    rpc_channel.cpp
)
