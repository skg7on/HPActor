# =============================================================================
# src/ref/CMakeLists.txt — Actor reference subsystem sources
# =============================================================================

target_sources(hpactor_lib PRIVATE
    actor_proxy.cpp
    actor_ref.cpp
)
