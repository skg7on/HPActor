# =============================================================================
# src/adt/CMakeLists.txt — ADT subsystem sources
# =============================================================================

target_sources(hpactor_lib PRIVATE
    dedup_cache.cpp
    json_helpers.cpp
    stream_buffer.cpp
)
